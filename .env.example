# Copy to .env and fill in to enable LLM triage (`cullwise triage --llm`).
# Cullwise works without this — it falls back to deterministic heuristics.
ANTHROPIC_API_KEY=

# Optional: override the Claude model used for triage.
# CULLWISE_MODEL=claude-sonnet-5
