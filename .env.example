# Copy to .env and fill in to enable LLM triage (`threatlens triage --llm`).
# ThreatLens works without this — it falls back to deterministic heuristics.
ANTHROPIC_API_KEY=

# Optional: override the Claude model used for triage.
# THREATLENS_MODEL=claude-sonnet-5
