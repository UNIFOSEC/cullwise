# Vulnerable demo (intentional)

These files contain **intentional, well-known insecure patterns** used only as a
scan target so the Cullwise live dashboard always has real Semgrep findings to
display. They are **not** part of the Cullwise package and are never imported.

Do not copy these patterns into real code — each one is exactly what Cullwise
is meant to catch.
